# Live Viewer

`LiveViewer` starts the server immediately and streams `push_state` calls to
connected browser tabs over WebSocket as a simulation runs. See
[Live Streaming](../usage/live-streaming.md) for a worked example.

::: simview.live.LiveViewer
