# State & Trajectories

`simview.state` defines the per-frame dynamic data in the
[JSON format](../dev/json-format.md): `SimViewBodyState` for one frame at a time, and
`BodyTrajectory` for authoring a whole `(T, B, ...)` trajectory in a single call.

::: simview.state
